version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis

  db:
    image: ankane/pgvector
    environment:
      POSTGRES_PASSWORD: postgres

  redis:
    image: redis